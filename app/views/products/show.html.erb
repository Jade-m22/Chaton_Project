<h1><%= @product.name %></h1>

<img src="<%= @product.image_url %>" alt="<%= @product.name %>" />

<p><strong>Prix :</strong> <%= number_to_currency(@product.price, unit: "€") %></p>

<p><%= @product.description %></p>

<% if @product.stock > 0 %>
  <p>🟢 En stock</p>
  <%= button_to "Ajouter au panier", cart_path(@product), method: :post %>
<% else %>
  <p>🔴 Rupture de stock</p>
<% end %>

<h2>Avis des clients</h2>
<%= render @product.reviews %>
