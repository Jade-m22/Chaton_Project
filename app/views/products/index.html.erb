<h1>Nos Produits</h1>

<div class="products-container">
  <% @products.each do |product| %>
    <div class="product-card">
      <h2><%= product.name %></h2>
      <p>Prix : <%= number_to_currency(product.price, unit: "€") %></p>
      <%= link_to "Voir le produit", product_path(product), class: "btn-show" %>
    </div>
  <% end %>
</div>


